import { DefaultTheme } from 'styled-components';

export const theme: DefaultTheme = {
    bgColor: '#000',
    textColor: '#fff',
    titleFont: "'Cormorant Garamond', serif",
    contentFont: "'Gowun Batang', serif",
};
